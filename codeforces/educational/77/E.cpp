#include <bits/stdc++.h>
using namespace std;
#define int  long long
const int LG = 18, KMX = 1 << 18 ;
int inp[KMX + 1000];
int n;
int dp[KMX][LG];
int32_t main(){
    cin >> n;
    for (int i = 0; i < n; i++ ){
        cin >> inp[i];
        __builtin_clz();
        in
    }
}
